{
  "name": "investors-ge-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.mjs && node --check shared.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
